server:
  port: 8080
  servlet:
    context-path: /identity 

spring:
  # Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/fooddelivery
    username: root
    password: 
  jpa: 
    hibernate: 
      ddl-auto: update
    show-sql: true

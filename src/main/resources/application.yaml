server:
  port: 8080
  servlet:
    context-path: /identity 

spring:
  # Database Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/fooddelivery
    username: root
    password: root
  jpa: 
    hibernate: 
      ddl-auto: update
    show-sql: true
